{
  "name": "docex-website",
  "version": "1.0.0",
  "description": "DocEX AI Website",
  "scripts": {
    "build": "mkdir -p public && cp -R *.html css js images components public/ && cp package.json public/"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-icons": "^4.2.0"
  }
}
